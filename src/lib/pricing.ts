/*
 * Pricing constants for the Pro purchase.
 *
 * Keep in sync with convex/purchases.ts (PRO_PRICE_USD / PRO_PRODUCT) —
 * the backend is the source of truth for the actual Checkout line item;
 * these drive the UI copy only.
 */
export const PRO_PRICE_USD = 9;
export const PRO_PRODUCT = "subtitle-toolkit-pro";
export const PRO_PRICE_LABEL = "$9";
